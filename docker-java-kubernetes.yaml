apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-demo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      service: hello-world
  template:
    metadata:
      labels:
        service: hello-world
    spec:
      containers:
        - name: hello-world-service
          image: java-hello-world-server:latest
          imagePullPolicy: Always
          env:
            #username
            - name: env.username
              valueFrom:
                configMapKeyRef:
                  name: myconfigmap
                  key: username
---
apiVersion: v1
kind: Service
metadata:
  name: service-entrypoint
  namespace: default
spec:
  type: LoadBalancer
  selector:
    service: hello-world
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
